{
  "name": "list-of-expenses",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run build --workspace=shared && npm run dev --workspace=server",
    "dev:client": "npm run build --workspace=shared && npm run dev --workspace=client",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server"
  }
}
